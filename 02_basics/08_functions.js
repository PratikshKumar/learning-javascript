

console.log(addOne(3));
// it is ok. we can access it
// but we cant do it for addTwo funtion before its inirialization


function addOne(n)
{
    return n+1;
}
const addTwo=funtion(n)
{
    return n+2;
}

